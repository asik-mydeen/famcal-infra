{
  "name": "nova-voice-proxy",
  "version": "1.0.0",
  "description": "WebSocket proxy for Amazon Nova 2 Sonic — adds auth headers that browsers can't send",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
